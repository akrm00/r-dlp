{$DOMAIN:localhost} {
    # API routes -> backend
    handle /api/* {
        reverse_proxy backend:3001
    }

    # Health check
    handle /health {
        reverse_proxy backend:3001
    }

    # Everything else -> frontend
    handle {
        reverse_proxy frontend:3000
    }
}
